@import '../../assets/styles/mixins.styl'

.pricingList
    font-size rem(28)
    padding-top rem(100)
    .inFixed
        position fixed
        left 0
        right 0
        top 0
        height rem(100)
        background-color #fff
        z-index 1